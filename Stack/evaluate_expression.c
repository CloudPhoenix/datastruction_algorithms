#include <stdio.h>
#include "stack.h"

void EvaluateExpression() {
  SqStack_c OPTR;
  InitStack_c(OPTR);
  Push_c(Sc, '#');

  SqStack_c OPND;
  InitStack_c(OPND);
  
  char ch = getchar();
  while(ch != '#') {
    
  }

}
